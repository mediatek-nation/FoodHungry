const express = require("express");
const router = express.Router();
const mongoose = require("mongoose");

// load model
const Payment = require("../../models/Payment");

// user can pay for their orders

module.exports = router;
